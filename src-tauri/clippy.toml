# Stricter than clippy's default of 25. Note this is *cognitive* complexity,
# which weights nesting rather than counting branches, so the number is not
# directly comparable to the frontend's cyclomatic `complexity: max 10` in
# .oxlintrc.json even though both now read 10.
#
# Nothing is exempted, test modules included.
cognitive-complexity-threshold = 10
